language: python
python:
  - "2.7"
before_install:
  - pip install --upgrade pip setuptools wheel
  - pip install --only-binary=numpy,scipy numpy scipy
# command to install dependencies
install:
  - pip install .

# command to run tests
script: 
  - "python ./upkeep/qtim_test_master.py"